import { Box, Typography, AppBar } from "@mui/material";

const Footer = () => {
  return (
    <AppBar position="relative" sx={{ p: 1, mt: 2, textAlign: "center" }}>
      <Typography>Developed by carliey</Typography>
    </AppBar>
  );
};

export default Footer;
